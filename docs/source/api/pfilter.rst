Particle Filter
===============

The particle filter (bootstrap filter) is the core algorithm for computing
the likelihood of a POMP model. It is available as a method on the
:class:`~pypomp.pomp_class.Pomp` class.

.. automethod:: pypomp.pomp_class.Pomp.pfilter
