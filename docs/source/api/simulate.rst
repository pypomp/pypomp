Simulation
==========

Simulation generates sample trajectories and observations from a POMP model.
It is available as a method on the :class:`~pypomp.core.pomp.Pomp` class.

.. automethod:: pypomp.core.pomp.Pomp.simulate
