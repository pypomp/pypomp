Training
========

Gradient-based training uses a differentiable particle filter to optimize
model parameters via stochastic gradient descent. It is available as a method
on the :class:`~pypomp.pomp_class.Pomp` class.

.. automethod:: pypomp.pomp_class.Pomp.train
