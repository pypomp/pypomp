Simulation
==========

Simulation generates sample trajectories and observations from a POMP model.
It is available as a method on the :class:`~pypomp.pomp_class.Pomp` class.

.. automethod:: pypomp.pomp_class.Pomp.simulate
