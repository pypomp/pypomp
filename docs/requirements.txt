# Documentation-only dependencies.
#
# pypomp's own runtime dependencies are deliberately NOT listed here:
# .readthedocs.yaml installs the package itself from pyproject.toml, which is
# the single source of truth for them.
sphinx
furo

# Used by the narrative docs and examples, not by the package itself.
seaborn
matplotlib
