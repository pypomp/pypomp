Maximum Likelihood via Iterated Filtering
==========================================

Iterated Filtering (IF2) is a simulation-based algorithm for maximizing the
likelihood of a POMP model with respect to its parameters. It is available as
a method on the :class:`~pypomp.core.pomp.Pomp` class.

.. automethod:: pypomp.core.pomp.Pomp.mif
